# Testing

Use the following commands for local validation:

- pnpm install
- pnpm lint
- pnpm typecheck
- pnpm test
- pnpm build

The test suite covers the core portfolio content and navigation requirements.
